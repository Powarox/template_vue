import state from './options/state';
import * as actions from './options/actions';
import * as getters from './options/getters';
import * as mutations from './options/mutations';

export default {
    namespaces: true,
    state,
    actions,
    getters,
    mutations,
}
